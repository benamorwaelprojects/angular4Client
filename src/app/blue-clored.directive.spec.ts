import { BlueCloredDirective } from './blue-clored.directive';

describe('BlueCloredDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueCloredDirective();
    expect(directive).toBeTruthy();
  });
});
